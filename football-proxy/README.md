# ⚽ Football Proxy API

Proxy em FastAPI para aceder à API-Football com IP fixo via Render.com.

---

## 🚀 Deploy no Render

1. Cria um novo repositório no GitHub chamado `football-proxy`.
2. Coloca estes 4 ficheiros:
   - proxy_apifootball.py
   - requirements.txt
   - render.yaml
   - README.md
3. Vai a [https://render.com](https://render.com) → “New Web Service”.
4. Liga a tua conta GitHub e escolhe o repositório `football-proxy`.
5. O Render deteta automaticamente o `render.yaml` e cria o serviço.
6. Depois do deploy, terás um domínio como:
